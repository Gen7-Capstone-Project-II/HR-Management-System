// ignore_for_file: constant_identifier_names, camel_case_types

enum E_CONTACT_TYPE { PHONE, MAIL, URL }

enum E_DIALOG_TYPE { ERROR, SUCCESS, INFO, WARNING, NONE }

enum E_LOADING_STATE { DONE, LOADING, WAITING, ERROR }

enum E_KEYBOARD_TYPE { TEXT, NUMBER, EMAIL }

enum E_TEXTFIELD_MODE { LABEL_TEXT, HINT_TEXT }

enum E_FORM_CONTROL_TYPE { TEXT_FIELD, DATE_PICKER, BOTTOM_SHEET, RADIO }

enum E_ANIMATED_BUTTON_STYLE { ROW, COLUMN }

enum E_MESSAGE_TYPE { DEFAULT, HISTORY }

enum E_ANIMATE_TYPE { OPACITY, TRANSLATE_Y }

enum E_DEVICE_TYPE { MOBILE, TABLET, DESKTOP }

enum E_SCREEN_TYPE { SMALL, NORMAL, LARGE }

enum LoadingStatus { none, loading, error, done }

enum Shift { full, morning, afternoon }

enum LeaveStatusEnum { all, pending, approved, rejected }
